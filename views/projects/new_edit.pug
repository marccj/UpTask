extends ../layout

include mixins/form 

block content
    .contenido-principal
        h1 #{pageTitle}
        
        // error-handling
        if errors
            each error in errors
                .alerta.error= error.text
        
        +Project(project)