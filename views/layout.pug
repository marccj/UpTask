<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="/css/app.css")
        link(rel="stylesheet", href="/css/normalize.css")
        script(src='https://kit.fontawesome.com/8141637bec.js')
        
        title #{pageTitle}
    body
        header.barra
            h1 UpTask - Administrador de Proyectos #{year}
            a(href="#") Cerrar Sesión
        
        main.contenedor
            aside.contenedor-proyectos
                .panel.crear-proyecto
                    a(href="/nuevo-proyecto" class="boton") Nuevo Proyecto
                        span +
                .panel.lista-proyectos
                    h2 Proyectos
                    ul#proyectos.proyectos
                        if(projects.length)
                            each project in projects
                                li
                                    a(href=`/proyectos/${project.url}`) #{project.name}                 
            block content
            //pre= vardump(projects)
        script(src="/dist/bundle.js")
